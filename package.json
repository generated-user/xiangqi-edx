{
  "name": "xiangqi-edx",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "pack-embed": "rm -f xiangqi-embed.zip && zip -r xiangqi-embed.zip embed src public package.json README.md"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "fairy-stockfish-nnue.wasm": "^1.1.11"
  }
}